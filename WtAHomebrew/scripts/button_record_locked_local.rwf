<DocumentElement xmlns="http://tempuri.org/Ruleset.xsd">
  <Scripts>
    <ScriptName>button_record_locked_local</ScriptName>
    <FolderID>32</FolderID>
    <Script>function isLocked()
    local bValue = (getValue()==1) --Debug.comment()  I just don't like this.
    return bValue;
end

function lock(bValue)
	if bValue then
		setValue(1);
	else
		setValue(0);
	end
end</Script>
    <RegisterScript>true</RegisterScript>
    <Encoding />
  </Scripts>
</DocumentElement>